non-fluents simple_network {
	domain = simple_compromise;

	objects{
		host: {h1, h2, h3, h4, h5};
		credentials: {c1, c2, c3, c4, c5};
	};

	non-fluents {
		CONNECTED(h1, h2);
		CONNECTED(h4, h5);
		CONNECTED(h3, h4);
		CONNECTED(h2, h3);
		ACCESSES(c1, h1);
		ittc_crack_attempt(c1) = 2;
		ACCESSES(c2, h2);
		ittc_crack_attempt(c2) = 0;
		ACCESSES(c3, h3);
		ittc_crack_attempt(c3) = 2;
		ACCESSES(c4, h4);
		ittc_crack_attempt(c4) = 0;
		ACCESSES(c5, h5);
		ittc_crack_attempt(c5) = 0;
		STORES(h1, c1);
		STORES(h1, c2);
		STORES(h2, c3);
		STORES(h3, c4);
		STORES(h4, c5);
	};
}

instance simple_network_instance {
	domain = simple_compromise;
	non-fluents = simple_network;

	init-state{
		compromised(h1) = true;
		rttc_crack_attempt(c1) = 1;
		rttc_crack_attempt(c2) = 1;
		rttc_crack_attempt(c3) = 2;
		rttc_crack_attempt(c4) = 2;
		rttc_crack_attempt(c5) = 1;
		value(h1) = 16;
		value(h2) = 1;
		value(h3) = 5;
		value(h4) = 16;
		value(h5) = 9;
	};

	max-nondef-actions = 1;
	horizon = 1000;
	discount = 1.0;
}